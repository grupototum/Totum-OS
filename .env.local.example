# Variáveis usadas pelos scripts da Alexandria.
# Copie este arquivo para .env.local (já está no .gitignore) e preencha
# os valores reais. Os scripts em scripts/alexandria-*.mjs carregam
# .env.local automaticamente.

# Supabase — projeto Apps Totum (cgpkfhrqprqptvehatad)
SUPABASE_URL=https://cgpkfhrqprqptvehatad.supabase.co

# JWT iniciando com "eyJ..." (~218 chars). Pegue em:
#   Dashboard → Project Settings → API → "service_role" → Reveal
# NUNCA commit este valor. NUNCA exponha no frontend.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Google AI Studio — chave usada para embeddings text-embedding-004 / 001
# Obter em: https://aistudio.google.com/apikey
GEMINI_API_KEY=AIzaSy...
